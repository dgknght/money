-if @first
  :javascript
    $(function() {
      $('.budget_monitor').budget_monitor({daysAvailable: #{@first.available_days}, daysPast: #{@first.past_days}});
    });

.budget_monitors
  .row
    .col-md-6.col-md-offset-3
      %h2#page_title Budget monitors

      -@budget_monitors.each do |budget_monitor|
        =render 'graph', budget_monitor: budget_monitor
      .actions
        =link_to 'Add', new_entity_budget_monitor_path(@entity), class: 'add_button', title: 'Click here to add a new budget monitor.'
