$(function() {
  // TODO Need to figure out why this is breaking the single page date pickers
//  $('.date_field').datepicker();
});

$(function() {
  // If javascript is enabled, hide the HTML only nav and show the ajax nav
  $('#html_nav').hide();
  $('#ajax_nav').show();
});
